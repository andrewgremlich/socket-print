// TODO: this class will hold the state of the socket print.
// UI will interface with state
// WEBGL renderer will pull the state.
export class Socket {
	constructor() {
		console.log("Socket constructor");
	}
}
